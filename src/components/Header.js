import Nav from './Nav';
import Logo from './Logo';

const Header = () => {
  return (
    <header className="app-header content-container">
      <Logo />
      <Nav />
    </header>
  );
};

export default Header;
